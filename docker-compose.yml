version: '3.4'

services:
  todofinal:
    image: todofinal
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 8002:8002
